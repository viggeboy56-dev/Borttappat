"use client";
import { useState,type FormEvent } from "react";
import { useRouter } from "next/navigation";

export function CreateSchoolForm(){const router=useRouter();const [busy,setBusy]=useState(false);const [error,setError]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);const response=await fetch("/api/operator/schools",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:form.get("name"),slug:form.get("slug")})});const result=await response.json() as {id?:string;error?:string};setBusy(false);if(!response.ok||!result.id){setError(result.error??"Skolan kunde inte skapas.");return;}router.push(`/operator/schools/${result.id}`);router.refresh();}return <form onSubmit={submit} className="rounded-xl border border-zinc-200 bg-white p-5"><h2 className="font-semibold">Skapa skola</h2><div className="mt-4 grid gap-4 sm:grid-cols-[1fr_1fr_auto] sm:items-end"><label className="text-sm font-medium">Skolnamn<input name="name" required minLength={2} maxLength={120} className="mt-1 block min-h-11 w-full rounded-lg border border-zinc-300 px-3 font-normal"/></label><label className="text-sm font-medium">Identifierare<input name="slug" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*" placeholder="exempelskolan" className="mt-1 block min-h-11 w-full rounded-lg border border-zinc-300 px-3 font-normal"/></label><button disabled={busy} className="min-h-11 rounded-lg bg-emerald-800 px-5 text-sm font-semibold text-white disabled:opacity-60">{busy?"Skapar…":"Skapa"}</button></div>{error?<p role="alert" className="mt-3 text-sm text-red-700">{error}</p>:null}</form>}
